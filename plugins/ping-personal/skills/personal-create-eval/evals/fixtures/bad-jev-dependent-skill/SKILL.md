---
name: bad-jev-dependent-skill
description: Otherwise-healthy fixture whose eval calls the optional personal-jev skill (expect HIGH optional_dependency, exit 1).
---

# bad-jev-dependent-skill

A minimal fixture. It ships a complete eval (eval-plan.md + an eval.ps1 with a real check),
so audit_eval.py reports it healthy. Used only by personal-create-eval's own eval.
