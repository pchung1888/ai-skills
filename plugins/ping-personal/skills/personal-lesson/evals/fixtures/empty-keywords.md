<!-- Intentionally empty override fixture. Passed as --keywords-file in eval.ps1
so the classifier routing assertions do NOT depend on whatever .claude/lessons-keywords.md
happens to exist in the launch directory (hermeticity). No table rows -> no override. -->
